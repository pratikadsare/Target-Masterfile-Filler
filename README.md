# Target Masterfile Filler — Interactive Mapping (Final, No Colors)

- No UI colors or live clearing; selections stick immediately.
- Mapping grid persists across reruns; only rebuilds if headers change.
- Duplicates resolved **on download** (latest edit wins) if enabled (default ON).
- First sheet only, row-1 headers, write from row-3; other sheets preserved; Excel duplicate highlight for Partner SKU & Barcode.
- Filename input, auto-map exact/fuzzy, import/export mapping.
