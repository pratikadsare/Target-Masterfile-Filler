# Target Masterfile Filler — Interactive Mapping (Sticky Green)

Changes:
- Default **Validate on Download** (no clearing while editing). Optional **Live (latest edit wins)** mode.
- **Green** mapped status, amber duplicates, red unmapped with a Status column and colored template list.
- **Scroll position preserved** across reruns.
- Keeps all previous behaviors: first sheet only, row-3 write, duplicate highlight (Partner SKU, Barcode), filename input, auto-map exact/fuzzy, import/export mapping.
